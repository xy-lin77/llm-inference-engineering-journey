# GPTQ 核心原理
1. 逐层量化：模型逐层独立量化，防止全局误差累积
2. Hessian 矩阵：通过激活 xx^T 近似，计算每列权重的输出敏感度
3. 逐列贪心：按**敏感度从高到低**依次量化；每列遍历16种INT4选最优，量化误差传递给下一列抵消
4. 分组量化：group_size=128，每组独立计算scale（对称量化），适配大模型权重分布

# 精度对比（Qwen-72B）
- RTN-4bit：MMLU 掉点 5~7%，不可用
- GPTQ-4bit(g128)：MMLU 掉点 1~2%，接近原模型精度
